<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.Admin.view.TabView.Venue.AdminViewTabVenueFragment">

    <!-- TODO: Update blank fragment layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <EditText
            android:layout_width="match_parent"
            android:background="@drawable/teacher_recyclerview_background_cell"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:hint="Search"
            android:id="@+id/editText_admin_view_search"
            android:padding="10dp" />
        <!-- Recycler view Call -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/Recycerview_admin_view_Venue"
            android:layout_width="match_parent"
            android:orientation="horizontal"

            android:layout_height="match_parent" />
    </LinearLayout>
</FrameLayout>